#include<iostream>
#include<vector>
#include<
vector<char> fun(vector<int> in,vector<char> ch)
{
    vector<char> ans;
    vector<pair<int,char>> temp;
    for

}
int main()
{
    vector<vector<int>> vec;
    vector<int> ans;
    int rows,columns;
    cin>>rows>>columns;
    for (int i = 0; i < row; i++)
    {
        vector<int> temp;
        for (int j = 0; j < column; j++)
        {
            int t;
            cin>>t;
        }
        vec.push_back(temp);       
    }
    for (int i = 0; i < row; i++)
    {
        for (int j = 0; j < column; j++)
        {
            sum[j] += vec[i][j];
        }      
    }
    int min = INT32_MAX;
    for(int i=0;i<)

    
}